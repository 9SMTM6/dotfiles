Host trebon
	User smaier

Host tarbean
	User smaier

Host lethani
	User smaier

Host vintas
	User smaier

Host archive
	User root

Host tcml
	HostName tcml-master01.uni-tuebingen.de
	User stud230

Host *
	# reuse existing ssh connections (see man ssh_config)
	# creates a file according to ControlPath for every (now shared) session
	ControlMaster auto
	ControlPath ~/.ssh/ssh_mux_%h_%p_%r
	AddKeysToAgent confirm
	# UseKeychain yes
